# SOLVER-Ralph UI Dockerfile
#
# Multi-stage build for the React UI.
# Produces a minimal nginx image serving static files.

# Build stage
FROM node:20-alpine AS builder

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Build for production
RUN npm run build

# Runtime stage
FROM nginx:1.27-alpine

# Copy nginx configuration
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf 2>/dev/null || true

# Copy built assets from builder
COPY --from=builder /app/dist /usr/share/nginx/html

# Create a script to inject runtime environment variables
RUN echo '#!/bin/sh' > /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo 'set -e' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo 'if [ -n "$VITE_API_URL" ]; then' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo '  sed -i "s|__VITE_API_URL__|$VITE_API_URL|g" /usr/share/nginx/html/index.html' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo 'fi' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo 'if [ -n "$VITE_OIDC_ISSUER" ]; then' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo '  sed -i "s|__VITE_OIDC_ISSUER__|$VITE_OIDC_ISSUER|g" /usr/share/nginx/html/index.html' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    echo 'fi' >> /docker-entrypoint.d/40-envsubst-on-index.sh && \
    chmod +x /docker-entrypoint.d/40-envsubst-on-index.sh

# Expose port
EXPOSE 80

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD wget -q --spider http://localhost/ || exit 1
